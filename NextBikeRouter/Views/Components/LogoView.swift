import SwiftUI

struct AppLogoView: View {
    var body: some View {
        VStack {
            Image("BikeCircular")
                .resizable()
                .frame(width: 100, height: 100)
        }
    }
}
